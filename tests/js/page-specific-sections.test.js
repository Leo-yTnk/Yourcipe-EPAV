import {describe,expect,it} from 'vitest';import{readFileSync}from'node:fs';const migration=readFileSync('supabase/031_catalog_pages_and_sections.sql','utf8'), imp=readFileSync('supabase/032_normalized_catalog_import.sql','utf8');describe('page-specific catalog sections',()=>{it('uses first-class composite page sections',()=>{expect(migration).toContain('unique (page_id, slug)');expect(imp).toContain("x->>'page',public.slugify(x->>'name')");});it('scopes replace_all and preserves fixed pages',()=>{expect(imp).toContain("x->>'page'=p.key");expect(imp).not.toContain('update public.catalog_pages');});it('restricts relationship types by page',()=>{expect(imp).toContain("not in ('home','recipes')");expect(imp).toContain("v_row->>'page'<>'products'");});});
