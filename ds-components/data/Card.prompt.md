Generic surface container with optional image, title, subtitle, and meta line — flexible enough to show a recipe (image, name, "20 min · Easy").

```jsx
<Card imageSrc="/soup.jpg" title="Weeknight Ramen" subtitle="Japanese · Noodles" meta="20 min · Easy" onClick={openRecipe} />
```
