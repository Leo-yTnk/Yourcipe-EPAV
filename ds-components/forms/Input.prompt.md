Labeled single-line text field with focus, error, and disabled states.

```jsx
<Input label="Recipe title" placeholder="e.g. Weeknight Ramen" />
```

Pass `error` to show a red border + message, or `helper` for a neutral hint.
