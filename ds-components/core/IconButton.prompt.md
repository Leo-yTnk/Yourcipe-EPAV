Circular icon-only button for toolbars and cards (favorite, share, close).

```jsx
<IconButton icon="♥" variant="secondary" aria-label="Save recipe" />
```

No icon set is bundled with this design system — pass any glyph, emoji-free unicode symbol, or your own SVG as `icon`.
